@startuml GestureGPT Architecture
!includeurl https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Container.puml

LAYOUT_WITH_LEGEND()

Person(user, "User", "Requests sign language video responses")

System_Boundary(gesturegpt, "GestureGPT") {
  Container(api, "GestureGPT API", "FastAPI", "OpenAI-compatible endpoints\n- Video lookup\n- Response formatting")
}

System_Ext(llm, "LLM Server", "Generates ASL-friendly text responses\n(OpenAI / Claude / Ollama / LM Studio)")

System_Ext(video_repo, "ASL Video Repository", "Pre-recorded sign videos\n(WLASL / ASL-LEX / Third-party)")

' Main flow
Rel(user, api, "POST /v1/chat/completions\n{messages: [...], format: 'mp4'}")
Rel(api, llm, "Generate text response")
Rel(llm, api, "ASL-friendly text\n'HELLO I FEEL GOOD'")
Rel(api, video_repo, "Lookup videos\n['HELLO', 'I', 'FEEL', 'GOOD']")
Rel(video_repo, api, "Video URLs")
Rel(api, user, "JSON + video URLs\n['/videos/HELLO.mp4', ...]")

note right of api
  **Processing Flow:**
  1. Receive user message
  2. Get ASL-friendly text from LLM
  3. Lookup videos from repo
  4. Return video URLs + text
end note

@enduml
